# Acceptance Criteria

- Existing code remains available in the new structure.
- Existing model artifact loading is preserved.
- Feature engineering tests pass.
- Python syntax checks pass.
- Training smoke test runs in test mode.
- CI references valid relocated paths.
- Repository structure is documented.
- Migration report exists.
- AI agent rules exist in `AGENTS.md`.
