# Status

## State

Planned

## Notes

The primary artifact `models/artifacts/best_pipeline.joblib` is intentionally ignored by Git because of size. Tests may use the tracked smoke-test artifact when necessary.
