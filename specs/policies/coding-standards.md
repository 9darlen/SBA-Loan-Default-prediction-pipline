# Coding Standards

- Prefer small, behavior-preserving changes.
- Keep training and inference responsibilities separated.
- Use explicit paths rooted at the repository when scripts move across directories.
- Preserve compatibility with existing serialized artifacts unless a planned Unit replaces them.
