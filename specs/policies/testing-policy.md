# Testing Policy

- Run unit tests after moving Python modules.
- Run syntax checks after import/path changes.
- Run a smoke training test when training entry points change.
- Add regression tests before intentional model behavior changes.
